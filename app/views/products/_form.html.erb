<%= simple_form_for(product) do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :category, collection: Product::CATEGORIES %>
  <%= f.input :photo, as: :file %>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
